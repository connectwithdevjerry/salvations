import noProviderBranching from './no-provider-branching.js';

/** Local ESLint plugin holding architecture-enforcing rules. */
export default { rules: { 'no-provider-branching': noProviderBranching } };
